# pi-ide-desktop 1.0.0 发布说明

发布日期：2026-05-12
分支：`main`

## 本次 v1.0.0 更新重点

- 默认界面配色改为黑灰中性风格，废弃旧版蓝色主题。
- 顶部操作区已简化，移除 `清空输出`、`复制输出`、`发送 /tree` 三个低频按钮。
- Windows Release 包已设置 GUI 子系统，避免双击启动时额外弹出控制台窗口。
- 保留核心操作：启动 / 停止 Pi、原生终端模式开关、停止全部 Pi。
- 保持多项目、多会话、目录树、会话文件、Pi 文件事件追踪等能力。

## 版本定位

`pi-ide-desktop` 1.0.0 是面向 Pi Coding Agent 的桌面 IDE 验收版本。该版本聚焦于 Windows 桌面端使用体验，提供项目管理、Pi 终端承载、增强会话输入、目录树、会话文件追踪与安装包交付能力。

## 核心功能

### 界面与视觉

- 默认配色已切换为黑灰中性风格。
- 侧边栏、面板、输入框、终端、按钮统一使用低饱和黑灰色。
- 选中态改为灰色胶囊高亮，减少大面积蓝色干扰。
- 终端 xterm 背景同步为黑灰风格。
- 顶部工具栏移除低频按钮，界面更接近轻量 AI Coding 工作台。

### Pi 启动与终端

- 内置 Tauri + PTY 承载 Pi CLI。
- 支持选择项目工作目录后启动 Pi。
- 支持多行启动命令，例如先设置环境变量再执行 Pi：

  ```bat
  set DEEPSEEK_API_KEY=sk-XXXXXXXXXXXX
  pi -nc
  ```

- Windows 多行启动脚本已改为 `.bat`：

  ```text
  ~/.pi-ide/launch-scripts/pi-launch-<run-id>.bat
  ```

- 修复 Windows 启动脚本路径带引号时可能被 `cmd.exe` 误解析的问题。
- 支持原生终端模式开关。
- 支持停止当前会话 Pi 或停止全部 Pi。
- 顶部已移除低频操作按钮：`清空输出`、`复制输出`、`发送 /tree`，降低界面复杂度。

### 项目与会话

- 左侧项目列表支持添加项目目录。
- 每个项目下支持创建多个 Pi 会话。
- 会话输出会保存到本地项目会话记录中。
- 项目右键菜单支持：
  - 在资源管理器打开
  - 删除项目
- 会话右键菜单支持：
  - 重命名
  - 删除

### 增强输入与文件附件

- 下方会话框支持多行输入。
- 支持 `Enter` 发送，`Ctrl/⌘ + Enter` 插入换行。
- 支持通过“插入文件”选择文件并加入会话文件。
- 支持从系统文件管理器拖入文件。
- 支持从右侧目录树拖入具体文件。
- 文件会以附件形式显示，发送时自动附加到提示词中。

### 右侧工具栏

- 右侧原命令历史/本地会话树已调整为可扩展工具栏。
- 工具栏支持展开/收起。
- 当前包含：
  - 目录树
  - 会话文件

### 目录树

- 支持展示当前项目目录结构。
- 支持点击文件夹展开/收起。
- 支持搜索文件或目录。
- 搜索支持：
  - 文件名匹配
  - 目录名匹配
  - 路径匹配
  - 匹配结果高亮
  - 自动展开匹配路径
  - 清空搜索
- 文件节点支持：
  - 双击使用系统默认程序打开
  - 拖动到会话框加入会话文件
- 大目录默认省略，降低卡顿风险：
  - `.git`
  - `node_modules`
  - `target`
  - `dist`
  - `build`
  - `.next`
  - `.vite`
  - `coverage`
  - `release`

### 会话文件追踪

- 右侧“会话文件”分为：
  - AI 参考文件
  - AI 输出文件
- 用户插入/拖入的文件会精确记录为 AI 参考文件。
- IDE 启动 Pi 时会自动注入项目级 Pi extension：

  ```text
  当前项目/.pi/extensions/pi-ide-file-tracker.ts
  ```

- Pi extension 会将结构化文件事件写入：

  ```text
  当前项目/.pi/pi-ide-file-events.jsonl
  ```

- 精确追踪规则：
  - Pi `read` 工具成功执行 → AI 参考文件
  - Pi `write` 工具成功执行 → AI 输出文件
  - Pi `edit` 工具成功执行 → AI 输出文件
- 对 `bash` 引起的文件变化，会通过 `git status --porcelain -z` 前后对比检测输出文件，标记为 `pi-tool-bash-diff` / `detected`。

## 验收建议

1. 启动应用并添加一个项目目录。
2. 使用多行启动命令启动 Pi。
3. 验证终端可以正常交互。
4. 打开右侧目录树，测试展开/收起和搜索。
5. 从目录树拖动文件到会话框，确认加入会话文件。
6. 让 Pi 读取文件，确认“AI 参考文件”出现 `pi-tool-read` 来源。
7. 让 Pi 修改/写入文件，确认“AI 输出文件”出现 `pi-tool-edit` 或 `pi-tool-write` 来源。
8. 右键项目，验证“在资源管理器打开”。

## 已知边界

- 如果 Pi 启动参数显式禁用了 extensions，例如 `--no-extensions`，则 AI 文件精确追踪不会生效。
- `bash` 内部读写文件无法像 `read/write/edit` 一样天然结构化确认，目前输出文件通过 git diff 检测，准确度低于内置工具事件。
- 非 Git 项目中，`bash` 文件变化检测能力有限；内置 `read/write/edit` 工具事件仍然准确。

## 构建与验证

本版本已通过：

```bash
npm run build
cargo check
npm run tauri:build
```
