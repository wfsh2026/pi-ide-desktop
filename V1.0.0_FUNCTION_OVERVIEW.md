# pi-ide-desktop V1.0.0 整体功能说明

本文档用于说明 `pi-ide-desktop` V1.0.0 验收版本的整体能力边界与主要功能。

## 1. 产品定位

`pi-ide-desktop` 是面向 Pi Coding Agent 的桌面 IDE 容器，目标是在 Windows 桌面端提供比命令行更易用的 Pi 使用体验。

V1.0.0 的核心定位：

- 将 Pi CLI 包装为桌面应用。
- 支持项目化管理与多会话管理。
- 支持增强输入、文件拖拽、目录树、会话文件追踪。
- 支持 AI 参考文件 / AI 输出文件的结构化展示。
- 提供接近结构化会话风格的黑灰专业界面。

## 2. Pi 启动与终端能力

### 2.1 Pi CLI 承载

- 使用 Tauri + PTY 承载 Pi CLI。
- 支持在桌面应用中查看 Pi 输出并与 Pi 交互。
- 支持原生终端输入模式开关。
- 支持停止当前 Pi 会话。
- 支持停止全部 Pi 进程。

### 2.2 多行启动命令

支持在启动命令框中输入多行 Windows 启动脚本，例如：

```bat
set DEEPSEEK_API_KEY=sk-XXXXXXXXXXXX
pi -nc
```

启动时会生成：

```text
~/.pi-ide/launch-scripts/pi-launch-<run-id>.bat
```

并通过 `cmd.exe /D /C` 执行。

### 2.3 Windows 启动体验

- Release 包使用 Windows GUI 子系统，避免双击启动时额外弹出控制台窗口。
- 启动脚本使用 `.bat`，避免部分 Windows 环境中 `.cmd` 调用异常。

## 3. 项目与会话管理

### 3.1 项目列表

左侧项目栏支持：

- 添加项目目录。
- 展开 / 收起项目。
- 查看项目下的 Pi 会话。
- 项目右键菜单。

项目右键菜单支持：

- 在资源管理器打开。
- 删除项目记录。

### 3.2 会话管理

每个项目下可创建多个 Pi 会话。

会话支持：

- 新建 Pi 会话。
- 选择历史会话。
- 保存会话输出。
- 重命名会话。
- 删除会话。
- 会话之间隔离终端输出与状态。

### 3.3 多会话 Pi 状态

V1.0.0 已具备多项目、多会话基础能力，并支持会话级 Pi 状态管理，包括：

- 当前会话是否已启动。
- 当前会话是否正在处理。
- 切换会话时同步对应状态。

## 4. 增强输入与文件附件

### 4.1 增强输入框

底部会话输入框支持：

- 多行输入。
- `Enter` 发送。
- `Ctrl / Cmd + Enter` 插入换行。
- 点击发送按钮发送。
- 正在处理时可切换为停止操作。

### 4.2 文件插入

支持通过“插入文件”按钮选择文件。

文件会显示在会话文件附件区域，并在发送时附加到提示词中。

### 4.3 文件拖拽

支持：

- 从系统文件管理器拖动文件到会话框。
- 从右侧目录树拖动具体文件到会话框。

拖入后会作为会话附件加入当前输入。

## 5. 右侧工具栏

右侧区域采用工具栏设计，替代早期的命令历史 / 本地会话树。

当前包含：

- 目录树
- 会话文件

工具栏支持展开 / 收起。

## 6. 目录树能力

### 6.1 项目目录展示

目录树展示当前项目文件结构。

支持：

- 文件夹展开 / 收起。
- 文件双击打开。
- 文件拖动到会话框。
- 刷新目录树。

### 6.2 目录树搜索

目录树顶部支持搜索框。

搜索能力包括：

- 按文件名搜索。
- 按目录名搜索。
- 按完整路径搜索。
- 搜索结果高亮。
- 自动展开匹配路径。
- 清空搜索。

### 6.3 大目录省略

为避免大项目卡顿，默认省略：

- `.git`
- `node_modules`
- `target`
- `dist`
- `build`
- `.next`
- `.vite`
- `coverage`
- `release`

## 7. 会话文件能力

右侧“会话文件”分为：

- AI 参考文件
- AI 输出文件

### 7.1 AI 参考文件

以下文件会进入 AI 参考文件：

- 用户通过“插入文件”选择的文件。
- 用户从目录树或系统文件管理器拖入的文件。
- Pi 成功调用 `read` 工具读取的文件。

### 7.2 AI 输出文件

以下文件会进入 AI 输出文件：

- Pi 成功调用 `write` 工具创建或写入的文件。
- Pi 成功调用 `edit` 工具修改的文件。
- Pi 通过 `bash` 间接造成 Git 工作区变化时检测到的文件。

### 7.3 Pi 文件事件追踪

IDE 启动 Pi 时会自动注入项目级 Pi extension：

```text
当前项目/.pi/extensions/pi-ide-file-tracker.ts
```

该扩展会将结构化文件事件写入：

```text
当前项目/.pi/pi-ide-file-events.jsonl
```

IDE 会读取这些事件并同步到当前会话文件列表。

## 8. UI 与视觉风格

V1.0.0 使用黑灰中性风格：

- 应用背景为深黑灰。
- 侧栏、面板、输入框使用低饱和灰色层级。
- 选中态使用灰色胶囊高亮。
- 终端同步黑灰配色。
- 顶部工具区保留核心操作，减少低频按钮干扰。

## 9. 当前已知边界

- 如果 Pi 启动参数显式禁用 extensions，例如 `--no-extensions`，会话文件精确追踪不会生效。
- `bash` 内部读写文件无法天然结构化确认，目前输出文件通过 Git diff 检测，准确度低于 `read/write/edit` 工具事件。
- 非 Git 项目中，`bash` 文件变化检测能力有限。
- 当前中央区域仍以终端输出为主，尚未实现完整结构化会话视图。

## 10. 验收建议

建议按以下路径验收：

1. 安装并启动 `pi-ide-desktop`。
2. 添加一个项目目录。
3. 使用多行启动命令启动 Pi。
4. 验证 Pi 终端交互。
5. 打开右侧目录树，测试展开 / 搜索 / 文件双击。
6. 从目录树拖动文件到会话框，确认进入附件列表。
7. 发送任务让 Pi 读取文件，确认 AI 参考文件出现记录。
8. 发送任务让 Pi 修改文件，确认 AI 输出文件出现记录。
9. 切换不同项目和会话，确认状态和输出隔离。
10. 验证安装包启动时没有额外控制台窗口。
