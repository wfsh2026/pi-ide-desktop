# pi-ide-desktop 2.0.0 发布说明

> **全面进化 — 从终端容器到结构化 AI IDE**

发布日期：2026-05-15
分支：`main`

## 版本定位

V2.0 标志着 pi-ide-desktop 从"Pi CLI 桌面容器"正式进化为**结构化 AI IDE 工作台**。自 V1.0 以来，经过数十个迭代版本，在会话视图、终端渲染、目录树、事件追踪、子 Agent 展示、拖拽交互等核心维度实现了全面重构与能力跃升。

## 2.0 核心亮点

### 🧠 结构化会话视图（V1→V2 最大变化）

- **思考分轮次展示** — AI 推理过程按轮次分组，可折叠展开
- **工具调用分组折叠** — `read` / `write` / `edit` / `bash` / `subagent` 按类型分组，点击展开详情
- **增强 Markdown 渲染** — 表格、代码高亮、列表、内联链接、引用区块完整支持
- **子 Agent (Subagent) 卡片** — 识别 `subagent` / `run_subagent` / `agent` 工具调用，生成可展开的子 Agent 执行卡片，展示子任务的思考、工具和结果
- **推理文本识别** — 自动分离 AI 推理过程与最终回答
- **文件参考/输出精确追踪** — AI 参考文件 + AI 输出文件，来源清晰（`pi-tool-read` / `pi-tool-write` / `pi-tool-edit` / `pi-tool-bash-diff`）

### 🖱️ 拖拽交互进化

- **目录文件拖拽到 Composer** — 支持从目录树或系统文件管理器拖拽目录/文件到输入区，自动解析路径并加入会话附件
- **拖拽视觉反馈** — 拖拽过程中显示悬浮预览卡片，光标变为 grab 状态
- **`dropPathModel` 智能路径解析** — 从拖拽文本中识别有效文件路径

### 💻 终端与交互重構

- **原生终端滚动方案** — 移除历史覆盖层，xterm viewport 原生滚动，流畅不跳動
- **终端 ANSI 渲染修复** — 颜色、TUI 光标、加粗/下划线完整渲染
- **多行启动命令** — 支持 `.bat` 启动脚本，环境变量 + Pi 命令一行搞定
- **会话预启动/预热** — 首次消息不再卡顿，预启动机制减少等待
- **会话间独立进程** — 切换会话不影响其他会话的 Pi 进程

### 📂 目录树重构

- **按需懒加载** — 大目录（`node_modules` / `.git` / `target` 等）自动省略，展开时才加载子目录
- **路径搜索+高亮** — 文件名/目录名/路径匹配，结果高亮显示，自动展开匹配路径
- **文件双击打开** — 使用系统默认程序打开文件

### 🧩 项目管理增强

- **会话归档** — 删除改为归档，后续可找回历史会话
- **会话重命名** — 右键支持
- **Pi 环境设置** — 检测 Pi 安装状态、版本、模型数量、配置路径，支持一键安装
- **Pi 模型切换控件** — 在 IDE 内切换模型
- **项目级配置** — `<project>/.pi.ide/config.json` 支持自定义启动命令、超时等

### ⚡ 性能与稳定性

- **目录树按需加载** — 避免大项目打开卡顿
- **会话切换性能优化** — 切换秒级响应
- **后台空闲自动关闭** — 默认 5 分钟无操作自动关闭 Pi 进程
- **Git 工作区变更检测** — bash 写入文件通过 `git status --porcelain -z` 精确对比
- **Pi extension 自动注入** — 启动 Pi 时自动注入 `pi-ide-file-tracker.ts`，无需手动配置

### 🎨 界面与视觉

- **黑灰中性风格** — 应用背景深黑灰，低饱和灰色层级，选中态灰色胶囊高亮
- **顶部工具栏精简** — 保留核心操作，移除低频按钮
- **右侧工具栏** — 目录树 + 会话文件，支持展开/收起

## 从 V1.0 到 V2.0 的完整变更

| 领域 | V1.0 | V2.0 |
|------|------|------|
| 会话视图 | 纯终端输出 | 结构化轮次 + 工具分组 + Markdown + Subagent 卡片 |
| 终端 | 基础 PTY 承载 | 原生滚动 + ANSI 修复 + 会话预启动 |
| 拖拽 | 仅系统文件拖入 | 目录拖拽 + 路径解析 + 视觉反馈 |
| 目录树 | 基础展开/收起 | 懒加载 + 搜索高亮 + 自动展开匹配路径 |
| 会话文件 | 基础追踪 | 精确事件映射 + git diff 检测 |
| 项目管理 | 新建/删除 | 归档 + 重命名 + 环境设置 + 模型切换 |
| 性能 | 大项目卡顿 | 按需加载 + 会话预热 + 空闲自动回收 |
| 扩展性 | — | Pi extension 自动注入 |

## 构建产物

```text
release/pi-ide-desktop.exe
release/pi-ide-desktop_2.0.0_x64-setup.exe
release/pi-ide-desktop_2.0.0_x64_en-US.msi
release/SHA256SUMS-2.0.0.txt
```

建议优先使用：

```text
release/pi-ide-desktop_2.0.0_x64-setup.exe
```

## 升级说明

V2.0 完全向后兼容 V1.x 的本地数据：

- 项目索引：`~/.pi-ide/projects.json`
- 会话日志：`~/.pi-ide/pi-sessions/`
- 启动脚本：`~/.pi-ide/launch-scripts/`
- 全局配置：`~/.pi-ide/config.json`
- 项目配置：`<project>/.pi.ide/config.json`

直接覆盖安装即可，无需手动迁移。

## 已知限制

- 仅支持 Windows x64
- 若 Pi 启动参数含 `--no-extensions`，文件精确追踪不生效
- `bash` 内部文件读写通过 git diff 检测，准确度低于 `read/write/edit` 工具事件
- 非 Git 项目中 bash 变更检测能力有限

## 致谢

感谢所有提供反馈和贡献的用户，让 pi-ide-desktop 从一个简单的终端包装器一步步成长为真正的 AI IDE 工作台。
